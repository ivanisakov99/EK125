fid=fopen('meatqute.dat');
fid2=fopen('somethingcool.dat');
fid3=fopen('whyphy.dat');
fid4=fopen('mynamejeff.dat');
maticemass=zeros(163,2);
aline=fgetl(fid);
for i=1:163
        [x y]=strtok(aline);
    x=str2num(x);
    y=abs(str2num(strtrim(y)));
    y=-log10(y);
    maticemass(i,1)=x;
    maticemass(i,2)=y;
    
    aline=fgetl(fid);
end


matnhtemp=zeros(18,2);
for i=1:152
aline=fgetl(fid2);
end
for i=1:18
        [x y]=strtok(aline);
    x=str2num(x);
    y=abs(str2num(strtrim(y)));
    y=log10(y);
    matnhtemp(i,1)=x;
    matnhtemp(i,2)=y;
    
    aline=fgetl(fid2);

end

matCO2=zeros(204,2);
for i=
aline=fgetl(fid3);
for i=1:204
        [x y]=strtok(aline);
    x=str2num(x);
    y=abs(str2num(strtrim(y)));
    y=log10(y);
    matCO2(i,1)=x;
    matCO2(i,2)=y;
    
    aline=fgetl(fid3);

end


matsealevel=zeros(266,2);
aline=fgetl(fid4);
for i=1:266
        [x y]=strtok(aline);
    x=str2num(x);
    y=abs(str2num(strtrim(y)));
    y=log10(y);
    matsealevel(i,1)=x;
    matsealevel(i,2)=y;
    aline=fgetl(fid4);
end


fclose('all');


plot(maticemass(:,1),maticemass(:,2),matsealevel(:,1),matsealevel(:,2),matnhtemp(:,1),matnhtemp(:,2),matCO2(:,1),matCO2(:,2))

